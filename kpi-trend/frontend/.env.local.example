# Базовый URL Python-бэкенда (FastAPI).
# Скопируйте файл в .env.local и при необходимости поменяйте порт/хост:
#   cp .env.local.example .env.local
# Переменная используется и серверным рендером Next.js, и кодом в браузере.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
